/*
 * utils.h
 *
 *  Created on: Feb 3, 2026
 *      Author: luke
 */

#ifndef INC_UTILS_H_
#define INC_UTILS_H_

#define CLAMP(val, min, max) ( (val) < (min) ? (min) : ( (val) > (max) ? (max) : (val) ) )

#endif /* INC_UTILS_H_ */
